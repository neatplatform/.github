# Settings

This directory includes specification for a GitHub organization settings.
These settings cannot be fully managed in an infra-as-code style currently,
so we just simply list them here for reference.
